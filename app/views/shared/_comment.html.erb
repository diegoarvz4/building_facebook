
    <%= form_for(:comment, url: comments_path) do |f|%>
        <%= f.text_field :content, placeholder: 'Enter your comment', class: 'comment-form' %>
        <%= f.submit 'send', class: 'comment-btn' %>
    <% end %>
